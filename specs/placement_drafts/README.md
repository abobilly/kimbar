# Placement Drafts

This directory contains AI-generated placement drafts based on
`docs/MISSING_ASSETS_SPEC.json`. Drafts are *not* consumed by the
runtime; they are meant as starting points for LDtk or manual placement.

Generate drafts:

```
node scripts/generate-placement-drafts.mjs
```

Validate drafts:

```
npm run validate
```
