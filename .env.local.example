# Enable Arcade Physics debug overlays in dev
VITE_PHYS_DEBUG=1

# External flashcard API URL (for production builds)
# In production, this points to the Cloudflare Worker that serves flashcards from R2
# For local development, leave this empty to use the local file
# VITE_FLASHCARD_API_URL=https://flashcard-api.YOUR-SUBDOMAIN.workers.dev/flashcards
